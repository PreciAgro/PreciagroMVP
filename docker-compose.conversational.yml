version: "3.9"

services:
  conversational-nlp:
    build:
      context: ./backend
      dockerfile: app/preciagro/packages/engines/conversational_nlp/Dockerfile
    env_file:
      - .env
    ports:
      - "8103:8103"
    depends_on:
      - redis
      - qdrant
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8103/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    labels:
      # Prometheus scrape annotation (if using docker_sd_configs)
      prometheus_job: "conversational-nlp"
      prometheus_port: "8103"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped

  qdrant:
    image: qdrant/qdrant:v1.8.3
    ports:
      - "6333:6333"
    environment:
      QDRANT__SERVICE__GRPC_ENABLED: "false"
    restart: unless-stopped
